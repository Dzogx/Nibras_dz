# تقرير تدقيق شامل — مشروع Nibras

**التاريخ:** 11 أوت 2026
**آخر نقطة تحقق (Checkpoint):** `3ba56cd` — "Added curriculum knowledge base RAG integration"
**حالة Git:** نظيفة، لا تغييرات غير ملتزمة

---

## 1. ما يعمل فعلياً الآن

| الميزة | الحالة | التفاصيل |
|--------|--------|----------|
| المصادقة | ✅ تعمل | Manus OAuth، جلسة آمنة، `protectedProcedure` على كل الإجراءات |
| الملف الشخصي | ✅ يعمل | الاسم، المادة (3 خيارات)، العام الدراسي، المدرسة، الولاية |
| إدارة الفصول | ✅ تعمل | إنشاء، تعديل، حذف، قائمة (CRUD كامل) |
| الخطط السنوية | ⚠️ نص حر | إنشاء + عرض + حذف. المحتوى نص عادي غير مهيكلة (لا مقاطع ككيانات) |
| إدارة الدروس | ✅ تعمل | إنشاء، تعديل، حذف، تحديد كـ"منجز"، ملاحظات |
| ملاحظات التدريس | ✅ تعمل | CRUD مع أنواع (عامة/صفية/تقويمية/تربوية) |
| مولّد الدروس (AI) | ✅ يعمل جزئياً | يولّد 5 أنواع محتوى + تفريد التعليم (6 خيارات). **لا يستخدم RAG رغم قبول البارامتر** |
| استوديو التقييم | ✅ يعمل | National Rules Engine + Teacher OS auto-import + RAG citations |
| مكتبة المحتوى | ✅ تعمل | حفظ، نسخ، تعديل، حذف، عرض تفصيلي |
| وضع المفتش | ✅ يعمل | مراجعة درس + تقييم بـ5 معايير، حفظ النتائج |
| واجهة عربية RTL | ✅ تعمل | Cairo + Noto Naskh Arabic، 10 أقسام في الشريط الجانبي |
| قواعد التقويم الوطنية | ✅ تعمل | `server/rules/nationalRules.ts` — محرك مركزي كامل |
| تصدير A4 | ✅ يعمل | طباعة مباشرة عبر `window.print` مع تنسيق A4 |

---

## 2. ما هو scaffold/prototype فقط

| العنصر | السبب |
|--------|-------|
| `Home.tsx` | لا يزال القالب الافتراضي ("Example Page") — غير مستخدم (يحوّل إلى /dashboard) |
| `curriculumSearchIndex` (جدول) | معرّف في schema لكنه **غير مستخدم إطلاقاً** في أي كود |
| `academicYears` (جدول) | معرّف لكن لا يوجد seed — المعلم يدخل السنة يدوياً |
| `studentCount` في classes | حقل موجود لكن لا يوجد جدول تلاميذ فردي |
| `tags` و `sourceDocumentIds` في aiResources | JSON مخزن لكن غير مستخدم في الواجهة |
| Dashboard | إحصائيات بسيطة فقط (عدادات) — لا رسوم بيانية ولا تحليل |

---

## 3. ما تم إنجازه منذ بدء العمل على Manus

| المرحلة | ما تم بناؤه |
|---------|-------------|
| MVP v1.0 (`9517167`) | 10 جداول DB، 12 tRPC router، 16 صفحة React، واجهة RTL، 17 اختبار |
| تفريد التعليم (`c552c4f`) | 6 خيارات تحكم في مولّد الدروس (مستوى، نمط تعلم، بلوم، نشاط، صعوبة، دعم) |
| محرك القواعد الوطنية (`31c222e`) | `nationalRules.ts` + تكامل Teacher OS + استشهادات إلزامية |
| RAG للتقييم (`3ba56cd`) | `getCurriculumForTopic` + حقن الوثائق + استشهادات per-question |

---

## 4. حالة Git

```
3ba56cd (HEAD → main)  RAG integration in assessment
31c222e                National Rules Engine + Teacher OS
c552c4f                Lesson Generator differentiation
9517167                MVP v1.0
6d1dbe7                Bootstrap
```
- **لا يوجد** Supabase — المشروع يستخدم **MySQL/TiDB** (Manus managed database)
- **لا يوجد** pgvector — البحث يتم عبر `LIKE` wildcard
- **لا يوجد** RLS — الأمان عبر `userId` filtering في tRPC procedures
- Working tree نظيف

---

## 5. حالة Knowledge Base و RAG

| المكوّن | الحالة |
|---------|--------|
| جدول `curriculumDocuments` | ✅ schema موجود ومُطبّق |
| `getCurriculumDocuments()` | ✅ بحث بالـ LIKE على title/content |
| `getCurriculumForTopic()` | ✅ بحث بالكلمات المفتاحية + فلتر بالمستوى والمادة |
| `curriculumSearchIndex` | ❌ جدول معرّف لكن **فارغ وغير مستخدم** |
| **محتوى فعلي في قاعدة المنهج** | ⚠️ **فارغ** — لا يوجد أي وثيقة منهاج محمّلة |
| OCR | ❌ غير موجود |
| pgvector / embeddings | ❌ غير موجود (MySQL لا يدعمه) |

**النقطة الحرجة:** RAG يعمل تقنياً لكنه **عديم الفائدة** بدون وثائق منهاج فعلية في قاعدة البيانات. النظام يتعامل مع هذا بذكاء — يعرض "لا توجد وثائق منهاج متاحة" ويطلب من المعلم الاستناد إلى المنهج العام.

---

## 6. حالة Teacher OS

| المكوّن | الحالة |
|---------|--------|
| الفصول | ✅ كامل (CRUD + studentCount) |
| الخطط السنوية | ⚠️ نص حر فقط (لا مقاطع/وحدات ككيانات مستقلة) |
| الدروس | ✅ كامل (CRUD + toggle completed + notes) |
| المقاطع (Sections) | ❌ غير موجود ككيان مستقل |
| الوضعية التعليمية | ❌ غير موجودة |
| الدفتر اليومي | ❌ غير موجود |
| `getTeacherOSContext()` | ✅ يجلب الدروس المنجزة + يستخرج الكفاءات تلقائياً |

---

## 7. حالة Assessment والتقويم التحصيلي

| المكوّن | الحالة |
|---------|--------|
| قواعد 1AM/2AM/3AM = 10+10 | ✅ في `nationalRules.ts` |
| قاعدة 4AM = 13+7 | ✅ في `nationalRules.ts` |
| التربية المدنية = 20/ساعة | ✅ في `nationalRules.ts` |
| التاريخ+جغرافيا = ساعة ونصف | ✅ في `nationalRules.ts` |
| `getAssessmentRule()` | ✅ |
| `getExamStructure()` | ✅ |
| `buildAssessmentContext()` | ✅ (يكوّن سياق التدريس الفعلي) |
| `getExamHeader()` | ✅ (الترويسة الرسمية) |
| `getBloomDistribution()` | ✅ |
| `COMPETENCY_CATEGORIES` | ✅ (5 فئات كفاءات) |
| `getTeacherOSContext()` | ✅ (استيراد تلقائي من Teacher OS) |
| Auto-import الدروس المنجزة | ✅ (checkbox في الواجهة) |
| استخراج الكفاءات المغطاة | ✅ (من objectives الدروس المنجزة) |
| ربط السؤال بالكفاءة | ⚠️ مطلوب في الـprompt لكن غير مضمون بدون structured output |
| سلم التنقيط | ✅ في الـresponse (pointDistribution) |
| نموذج الإجابة | ⚠️ يُولّد كجزء من المحتوى لكن ليس ككيان منفصل |
| تصدير Word | ❌ غير موجود |
| تصدير PDF | ❌ غير موجود (فقط A4 print) |

---

## 8. حالة تحليل النتائج والعلاج

| المكوّن | الحالة |
|---------|--------|
| جدول نتائج التلاميذ | ❌ **غير موجود** |
| تتبع درجات فردية | ❌ **غير موجود** |
| تحليل إحصائي | ❌ **غير موجود** |
| رسوم بيانية للتطور | ❌ **غير موجود** |
| العلاج والإثراء | ❌ **غير موجود** |

---

## 9. حالة Inspector

| المكوّن | الحالة |
|---------|--------|
| مراجعة درس | ✅ (LLM + 5 معايير + curriculum context) |
| مراجعة تقييم | ✅ (LLM + 5 معايير) |
| حفظ المراجعات | ✅ (inspectorReviews table) |
| عرض تاريخ المراجعات | ✅ |
| تدرج بلوم في التقييم | ⚠️ مطلوب في prompt لكن غير مضمون |

---

## 10. حالة AI الفعلية وجودة المخرجات

- **المحرك:** Manus built-in LLM API (`invokeLLM`)
- **لا يوجد** model selection صريح — يستخدم الـdefault
- **لا يوجد** structured output (JSON schema) — المخرجات Markdown نصية
- **لا يوجد** temperature/maxTokens تهيئة
- **الاستشهادات:** تعمل في Assessment Studio فقط، **غير مفعّلة في Lesson Generator**
- **generateLesson يقبل `curriculumDocs` لكن لا يستخدمه** — ثغرة معمارية

---

## 11. الاختبارات التي نجحت فعلياً

```
✓ server/routers.test.ts (30 tests) — mocks for db + llm
✓ server/auth.logout.test.ts (2 tests) — cookie clearing
Total: 32 tests passing
```

- جميع الاختبارات تستخدم `vi.mock` (لا اتصال حقيقي بقاعدة البيانات)
- **لا يوجد guard** يفشل الاختبار إذا تم استدعاء `getDb()` الحقيقي

---

## 12. الأخطاء والـBlockers الحالية

| الأولوية | المشكلة | الأثر |
|----------|---------|-------|
| **P0** | لا توجد وثائق منهاج في قاعدة البيانات | RAG بلا فائدة — لا استشهادات حقيقية |
| **P1** | `generateLesson` يقبل curriculumDocs ولا يستخدمه | مولّد الدروس يعمل بمعزل عن المنهج |
| **P1** | لا structured output من LLM | الاستشهادات غير مضمونة |
| **P2** | لا جدول تلاميذ/درجات | لا تحليل نتائج ولا علاج |
| **P2** | المقاطع غير مهيكلة في الخطط السنوية | لا يمكن ربط التقييم بمقطع محدد |
| **P3** | Home.tsx لا يزال template | غير مؤثر وظيفياً |
| **P3** | curriculumSearchIndex غير مستخدم | جدول ميت |

---

## 13. ما الذي أصبح جاهزاً لتجربتك كأستاذ

يمكنك الآن:

1. **تسجيل الدخول** وإنشاء ملفك الشخصي (المادة + السنة)
2. **إنشاء فصل** (مثلاً: السنة الرابعة متوسط، قسم 1)
3. **إنشاء خطة سنوية** (نص حر — تكتبها يدوياً)
4. **إضافة دروس** وتحديد بعضها كـ"منجزة"
5. **توليد محتوى** في Lesson Generator (بدون RAG حقيقي)
6. **إنشاء تقييم** — النظام يطبق قواعد النقاط تلقائياً + يستورد الدروس المنجزة + يبحث في المنهج (إن وُجد)
7. **تصدير A4** للطباعة
8. **مراجعة كمفتش** — تقييم درس أو تقييم
9. **مكتبة المحتوى** — حفظ، نسخ، تعديل

---

## 14. المسار الكامل: ما ينقص تحديداً

```
القسم → الخطة → المقطع → الوضعية → المذكرة → التنفيذ → التقويم → التحليل → العلاج
 ✅      ✅      ❌        ❌        ✅      ✅        ✅        ❌        ❌
```

**المفقود حرجاً:**

1. **كيان المقاطع (Sections)** — يجب أن تكون جدولاً مستقلاً مرتبطاً بالخطة السنوية
2. **كيان الوضعيات التعليمية** — مرتبط بالمقطع والدرس
3. **سجل نتائج التلاميذ** — جدول individual grades
4. **تحليل النتائج** — إحصائيات، رسوم بيانية، نسب النجاح
5. **العلاج والإثراء** — توليد تلقائي بناءً على نتائج التحليل
6. **وثائق المنهج الحقيقية** — يجب تحميل المنهاج الجزائري الرسمي

---

## 15. النسبة التقريبية للنواة الوظيفية

| المجال | النسبة |
|--------|--------|
| Teacher OS (فصول، دروس، ملاحظات) | **85%** |
| AI Generation (درس + تقييم) | **70%** |
| Curriculum Knowledge Base | **20%** (بنية جاهزة، محتوى فارغ) |
| Assessment/Rules Engine | **90%** |
| Inspector | **75%** |
| Content Library | **80%** |
| Results Analysis | **0%** |
| Treatment/Remediation | **0%** |
| **الإجمالي التقريبي** | **~60%** |

---

## 16. أقرب نقطة يمكنك عندها تجربة Nibras

**الآن — مباشرة.** يمكنك:
- تسجيل الدخول وإنشاء فصل
- إضافة دروس وتحديد المنجز
- توليد تقييم مع تطبيق قواعد النقاط الوطنية تلقائياً
- طباعة A4

**لكن بدون وثائق المنهج، التقييم لا يستشهد بمرجع حقيقي.**

---

## 17. قائمة المهام ذات الأولوية الأعلى (P0–P1)

1. **تحميل وثائق المنهج الجزائري** — وحدات، كفايات، دروس لكل مستوى ومادة (P0)
2. **إصلاح generateLesson لاستخدام RAG** — يربط مولّد الدروس بقاعدة المنهج (P1)
3. **Structured output من LLM** — ضمان استشهادات مضمونة عبر JSON schema (P1)
4. **جدول المقاطع (Sections)** — كيانات مستقلة مرتبطة بالخطة السنوية (P1)
5. **جدول التلاميذ والدرجات** — individual student records (P1)
6. **تحليل النتائج** — إحصائيات ورسوم بيانية (P2)
7. **العلاج والإثراء** — توليد تلقائي بناءً على التحليل (P2)
8. **تصدير PDF** — إضافة بجانب الطباعة A4 (P2)
9. **حذف curriculumSearchIndex أو تفعيله** — جدول ميت (P3)
10. **تحديث Home.tsx** — صفحة هبوط حقيقية (P3)

---

## 18. ما يجب عدم بناؤه الآن

- ❌ **OCR** — لا حاجة حتى يتم تحميل وثائق المنهج يدوياً
- ❌ **pgvector/embeddings** — MySQL لا يدعمه، والبحث النصي LIKE كافٍ حالياً
- ❌ **نظام إشعارات** — خارج نطاق MVP
- ❌ **تحليلات متقدمة** — Dashboard analytics
- ❌ **تعاون فريق/مؤسسات** — مستبعد صراحة
- ❌ **نظام دفع/اشتراكات** — مستبعد من MVP
- ❌ **دور المفتش كحساب منفصل** — Inspector Mode هو ميزة للمعلم فقط
