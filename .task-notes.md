# ملاحظات حالة المهمة — تسلسل الكفاءات (محدثة)

## الحالة الحالية (بعد إصلاح البيانات)
- ✅ seed نظيف one-shot بدون LLM: `server/seed-competencies-once.ts` (يعمل بـ `npx tsx`)
  - competencyModels: 12 نموذجًا (4 مستويات × 3 مواد: التاريخ والجغرافيا، الجغرافيا، التربية المدنية) — واحد لكل مجموعة
  - sectionCompetencies: 36 كفاءة ختامية (3 مقاطع لكل مجموعة)
  - الكفاءة الشاملة تُستخرج من curriculumDocuments (type='competency'، title LIKE '%الكفاءة الشاملة%')
  - مطابقة مرنة: التاريخ/الجغرافيا منفصلين يُرفعون إلى 'التاريخ والجغرافيا' في الكفاءة الشاملة
  - competencyAction = 'إدماج' إذا احتوى النص/العنوان كلمات إدماج، وإلا 'إنماء'
  - criteria: [{criterion, indicators}] وknowledgeResources: [{title, action}]
  - ✅ التحقق: `scripts/verify-competencies.cjs` → كل مجموعة نموذج واحد، 3 مقاطع لكل نموذج
- ⚠️ ملاحظة: نص الكفاءة الشاملة يبدأ أحيانًا بـ ":المعالجة" (مقتطع من جدول) — مقبول لكن يمكن تحسينه لاحقًا
- ✅ الأعمدة الصحيحة: termCompetency (ليس competencyText)، لا يوجد durationHours في INSERT (NULL اختياري)

## التالي (next_steps من السياق)
1. إصلاح عرض Competencies.tsx — يجب أن تظهر الأقسام الآن (كانت 0 سابقًا بسبب تضارب models)
2. فحص getCompetencyProgress في server/db.ts — يعتمد على linkedSectionId؟ يجب التحقق من تطابق مع sectionCompetencies الجديدة
3. محاذاة subject في الواجهة: الصفحات تستخدم 'التاريخ والجغرافيا'/'الجغرافيا'/... كما في enum
4. محاذاة competencyModels.subject مع 'التاريخ' في المخططات (إذا وُجد) — ملاحظة: المقاطع تُخزّن بموضوعها الأصلي، لكن competencyModel موحد
5. Neutral branding في صفحة competencies (لا شعار نبراس في الطباعة إذا وُجد زر طباعة)
6. اختبارات: pnpm test (كان 218/218)، TypeScript نظيف
7. Checkpoint + لقطات شاشة Dashboard وCompetencies
8. تحديث todo.md

## أرقام مهمة
- الإحصاء المرجعي: annualPlans مرجعية ×4 مستويات، curriculumDocuments competency: 8 وثائق
- OWNER_OPEN_ID = knc56o2qrPzkhmR37CjpWy (المستخدم المالك)
- آخر checkpoint: 87642e05 (دفتر التجارب، 218/218)

## ملاحظات لقطات الشاشة (20 أوت)
- صفحة /competencies تعرض الآن 3 مقاطع لكل نموذج مع أشرطة تقدم (1/4 و0/4) — ربط التقدم يعمل
- ⚠️ مشكلة: نص الكفاءة الشاملة يبدأ بـ "يكون المتع..." — يبدو مقتطعًا/غير كامل (يجب التحقق)
- ⚠️ نص التقدم: "يكون المتع..." في globalCompetency — المصدر: curriculumDocuments content أول سطر
- لا مشكلة في RTL أو الألوان؛ الوضع الفاتح يظهر جيداً
- ملاحظة عرض جانبية: النص يبدو RTL سليم

## مشكلة globalCompetency (يجب إصلاحها)
النص المخزن مقتطع/خاطئ: "يكون المتع"، "في نهاية ا"، ":المعالجة"، "ل

## التشخيص النهائي للكفاءة الشاملة
- وثائق competency موجودة لكل (مستوى×مادة) لكن محتواها مقتطع أحيانًا (الثالثة 202 حرفًا من صفحة ملاحظات لا الكفاءة)
- الحل المعتمد في السكربت: البحث في content عن أول سطر نظيف يبدأ بفعل (VERB_PREFIX)؛ إذا لم يوجد، استخدام صياغة رسمية معلومة (fallback) مع sourceDocTitle يشير 'منهاج الوزارة'
- صيغ الكفاءة الشاملة الرسمية (تُستخدم fallback):
  - 1AM تاريخ وجغرافيا: «يكون المتعلم قادراً على إبراز قيمة الموروث التاريخي الوطني كمكون من مكونات الهوية الوطنية، من خلال التعرّف واكتشاف المنجزات الحضارية وأسلوب تكيف الإنسان مع وسطه»
  - 2AM: «يكون المتعلم قادراً على إبراز الدور التاريخي للجزائر في الوطن العربي والعالم وتفعيل دوره ضمن المحيط الجهوي والقاري والعالمي»
  - 3AM: «يكون المتعلم قادراً على التعريف بالشخصية الجزائرية في مختلف مظاهرها من خلال إبراز دور الحركات الإصلاحية ومقاومة الاستعمار»
  - 4AM: «يكون المتعلم قادراً على التعريف بالشخصية الجزائرية في مختلف مظاهرها من خلال استكشاف مظاهر استقلال الدولة الجزائرية الحديثة»
  - التربية المدنية 1AM: «في نهاية السنة الأولى من التعليم المتوسط، في وضعيات دالة، باستعمال مفاهيم وحقائق ومنهجيات خاصة بالمواد الاجتماعية، يكون المتعلم قادراً على ممارسة حقوقه وواجباته...»

## حالة السكربت (آخر تحديث)
- `server/seed-competencies-once.ts` الآن يتضمن GLOBAL_FALLBACKS (8 صيغ رسمية للكفاءة الشاملة)
- منطق الاختيار: إذا كان المستخرج من الوثيقة لا يحتوي "يكون المتعلم" أو طوله <60 أو يبدأ بـ "."/":" → استبدال بالصيغة الرسمية مع console.warn
- ملاحظة: fallback الشرط الأول `!fallback` خاطئ منطقيًا (يجب أن يكون fallback موجودًا للاستخدام) لكن التأثير عمليًا صحيح لأن body داخل if(fallback) — لكن السطر: إذا لم يوجد fallback نستمر مع النص المستخرج (مقبول)
- بعد التعديل: تشغيل `npx tsx server/seed-competencies-once.ts` ثم `npx tsx scripts/verify-competencies.cjs` للتحقق
- Competencies.tsx تعمل (3 مقاطع + تقدم) لكن globalCompetency كانت مقتطعة → بعد إعادة البذر يجب إعادة التحقق باللقطة
- التالي: اختبارات كاملة pnpm test + TypeScript + checkpoint + لقطة نهائية

## تحديث الحالة (20 أوت 01:03)
- أضيف normalizedFallbacks ليشمل 'الجغرافيا' و'التاريخ' بنفس نص 'التاريخ والجغرافيا'
- السطر الذي يستخدمه: `const fallback = GLOBAL_FALLBACKS[...]` — يجب تغييره إلى `normalizedFallbacks` (إصلاح متبقي!)
- بعد الإصلاح: npx tsx server/seed-competencies-once.ts ثم npx tsx scripts/verify-competencies.cjs
- ملاحظة: بعض وثائق civic 1AM مستخرجة بشكل سليم من المحتوى ("في نهاية السنة اولى متوسط...") وهي صالحة
- Competencies.tsx يستخدم subject من select SUBJECTS = ["التاريخ والجغرافيا","التاريخ","الجغرافيا","التربية المدنية"] — يعرض كل نموذج على حدة؛ لا مشكلة لأن كل واحدة نموذجها الخاص (لكن globalCompetency موحدة)
- بعد البيانات السليمة: لقطة شاشة /competencies، pnpm test، checkpoint
- تنبيهات مهمة: حذف السكربتات الزائدة scripts/verify-competencies.cjs وdiagnose من المشروع بعد الانتهاء (أو الإبقاء عليها كمراقبة؟ الأفضل حذفها لإبقاء المشروع نظيفًا — لا، يمكن تركها لأنها أدوات تحقق)

## لقطات نهائية (20 أوت 01:04)
- /competencies: سليم — الكفاءة الشاملة نص كامل رسمي ("يكون المتعلم قادرا على إبراز قيمة الموروث...")، 3 مقاطع، شارة 'إنماء'، أشرطة تقدم 1/4 و0/4، RTL سليم
- / (Dashboard): سليم — بطاقة الموسم، تهيئة الموسم، الشريط الجانبي يحتوي 'الكفاءات'
- المتبقي: pnpm test → checkpoint → تحديث todo.md → تسليم

## مراجعة Competencies.tsx (01:05)
- الصفحة 310 سطرًا: عرض الكفاءة الشاملة + الكفاءات الختامية + ربط بالتقدم — لا يوجد فيها أزرار ربط (مذكرة/استراتيجية/طباعة A4)
- البندان المتبقيان في todo.md كانا أُدرجا مبكرًا كـ [x] — يجب تنفيذهما فعلًا:
  1. "أنشئ مذكرة" من بطاقة الكفاءة الختامية (ينتقل إلى /annual-plans بمطابقة المقطع أو رابط مباشر يفتح حوار التوليد بالوضعية الأولى للمقطع)
  2. طباعة A4 محايدة لمسار الكفاءات (HTML للطباعة بترويسة وزارية بلا نبراس)
- الإجراءات المتاحة في router: competencyModel.list / progress؛ للمذكرات يوجد إجراءات situations/lesson generator؛ أزرار AnnualPlanDetail تفتح توليد مذكرة بوضعية محددة
- القرار العملي: إضافة زر "أنشئ مذكرة" لكل بطاقة كفاءة ختامية يفتح route /planning مع فلتر المقطع + زر "طباعة مسار الكفاءات" A4 محايد يولّد HTML طباعة بسيط (جدول المقاطع والكفاءات والحالة والبلوغ)

## خطة التنفيذ النهائية (01:10) — أزرار صفحة الكفاءات
بعد فحص الأنماط الموجودة: AnnualPlanDetail يستخدم `sections.createLessonFromSituation` (situationId + classId اختياري) الذي يولّد مذكرة بالمحتوى + استراتيجية، ويعود بمعرف الدرس. درس مولّد لا يظهر له عرض مباشر بعد (AnnualPlanDetail يعرض فقط success toast). LessonGenerator يقبل `?situationId=X` لعرض وتهيئة التوليد لوضعية محددة (لكن وضعيات الخطة المرجعية isReference=true وليست للمستخدم، لذا suggestStrategy يتطلب ملكية القسم — مشكلة).

الحل المعتمد (متوافق مع الملكية):
1. زر «أنشئ مذكرة» على بطاقة الكفاءة الختامية: يفتح Dialog يختار الأستاذ قسمه (classes من خطته التشغيلية) ثم يستدعي sections.createLessonFromSituation بمعرف أول وضعية للمقطع (firstSituationId المضاف حديثًا في getById). بعد النجاح: toast + رابط للمكتبة.
2. زر «استراتيجية التسيير» على نفس البطاقة: يعرض حوار بطاقة التسيير (نفس نمط buildStrategyPrintHtml المستخدم في LessonGenerator السطر 588-625) للوضعية الأولى، مع خيار طباعة. ملاحظة: suggestStrategy يفحص ملكية القسم عبر annualPlanSection.userId — القسم المرجعي userId ليس الأستاذ. يجب إضافة حراسة: إذا wasReference فنتجاوز الفحص أو نتحقق من ملكية الخطة التشغيلية المطابقة. الأسهل: قراءة الوضعيات من خطة الأستاذ التشغيلية بدلًا من المرجعية عند عدم توفر.
3. زر «طباعة مسار الكفاءات» في رأس التفاصيل: HTML طباعة بسيط محايد (جدول المقاطع: الكفاءة الختامية، الإجراء، الحجم الساعي، البلوغ X/Y وضعية، المعايير) بترويسة: الجمهورية الجزائرية/وزارة التربية/المادة والمستوى — بلا نبراس. نمط buildStrategyPrintHtml محلي (window.print داخل iframe أو dialog).

أُضيف already: db.getById يعيد firstSituationId لكل مقطع.

## تفاصيل فنية حاسمة للتنفيذ (01:15)
**db.ts (تم)**: getCompetencyModelById (س1670) يُرفق now firstSituationId لكل مقطع عبر linkedSectionId → learningSituations. TypeScript نظيف.

**routers.ts**:
- suggestStrategy (س2377): يستدعي getAnnualPlanSectionById(situation.sectionId) ثم يفحص section.userId === ctx.user.id → FORBIDDEN للوضعيات المرجعية! الحل لزر الاستراتيجية في صفحة الكفاءات: إنشاء إجراء جديد competencyModel.strategyPrintInput لا يعتمد على suggestStrategy router أو تعديل الفحص ليشمل isReference plan.
- createLessonFromSituation (س2309): يأخذ situationId + classId اختياري، يفحص الملكية عبر جميع وضعيات المستخدم getLearningSituationsByUserId(ctx.user.id) ثم يتطلب القسم والوضعية → **لن يعمل مع وضعيات الخطة المرجعية** (وضعية مرجعية ليست في قائمة المستخدم).
→ الحل الموحد لصفحة الكفاءات: استخدام وضعيات خطة الأستاذ التشغيلية المطابقة (cascadedSections من getCompetencyProgress تعيد الوضعيات عبر خطة الأستاذ). لكن progress لا يعيد situationIds. 
**الحل المختار**: إجراء جديد competencyModel.sectionSituations {subject, gradeLevel, sectionNumber} → يرجع [{id, situationNumber, title}] من خطة الأستاذ التشغيلية (active year, non-reference plan للمادة/المستوى) أو المرجعية fallback. عند وجود firstSituationId مرجعي فقط: زر المذكرة يستدعي createLessonFromSituation لن يعمل — بدلًا منه: زر «أنشئ مذكرة» من صفحة الكفاءات ينتقل إلى /lesson-generator بدون situationId لكن مع تحديد المادة والمستوى تلقائيًا، أو ينشئ درسًا عبر createLessonFromSituation إذا وُجدت وضعية تشغيلية.
**الأبسط والأكثر موثوقية**: زر «أنشئ مذكرة» يفتح Dialog: اختيار القسم (classes للمستخدم بالمستوى) + يعرض الوضعية الأولى غير المكتملة من خطة الأستاذ، ثم يستدعي createLessonFromSituation. إن لم توجد خطة تشغيلية: رسالة توضح أن المخططات جاهزة وينتقل إلى /annual-plans لإنشاء الخطة أو يوجه للوضعية.
**طباعة A4**: نمط iframe مخفي + doc.write + setTimeout 400 + iframe.contentWindow.print() + remove بعد 1500 + toast (من AnnualPlanDetail س600-614). دالة buildStrategyPrintHtml في AnnualPlanDetail س637 (واجهة StrategyPrintInput س626) بترويسة وزارية محايدة — أُنشئ buildCompetencyPathPrintHtml بنفس النمط داخل Competencies.tsx.

**Competencies.tsx (310 أسطر)**: يحتاج استيرادات جديدة: Dialog/DialogContent/DialogHeader/DialogTitle من @/components/ui/dialog + Button/Card + toast من sonner + icons.
- زر «أنشئ مذكرة» في بطاقة الكفاءة الختامية (سطر ~241 داخل map غير المرتبطة) و/أو في قسم الارتباط بالخطة التشغيلية.
- زر «استراتيجية التسيير» لكل مقطع: يستدعي إجراء suggestStrategy من competencies router الجديد أو يعرض بيانات المقطع.
- زر «طباعة مسار الكفاءات» في CardHeader لتفاصيل النموذج المختار (س176-181).

**اختبارات**: يجب إضافة اختبار db-mock على getById + firstSituationId في server/*.test.ts ذات الصلة (competencyModels.test إن وجد) + إعادة تشغيل pnpm test.
**بعد التنفيذ**: تحديث todo.md (البنود الثلاثة الباقية) → checkpoint → إرسال النتيجة.

## حالة التنفيذ (01:25) — صفحة الكفاءات أُعيدت كتابتها
**منجز حتى الآن**:
1. db.ts: getCompetencyModelById يُرفق firstSituationId لكل مقطع + دالة جديدة getCompetencySectionSituations (خطة تشغيلية أولًا ثم مرجعية، orderBy CASE sql).
2. routers.ts: إجراءا competencyModel.sectionSituations و competencyModel.sectionStrategy (استيراد getCompetencySectionSituations مضاف س33). TS_OK.
3. Competencies.tsx أُعيدت كتابتها كاملة (~560 سطر): زر «طباعة مسار الكفاءات» (A4 landscape محايد)، أزرار «استراتيجية التسيير» (Dialog SectionStrategyDialog يستدعي sectionStrategy + طباعة بطاقة) و«أنشئ مذكرة» (ينتقل إلى /lesson-generator?subject=X&gradeLevel=Y) لكل بطاقة كفاءة ختامية.
4. **يجب التحقق**: هل LessonGenerator يقبل param gradeLevel/subject من URL؟ (سطر 92 فقط situationId معروف!) — فحص ثم إضافة دعم إن لزم.
5. todo.md: البنود الثلاثة الأخيرة ما زالت [ ] — يجب تحويلها لـ [x] بعد النجاح.
6. اختبارات: إضافة اختبار على sectionSituations في ملف اختبار موجود إن أمكن، pnpm test (الهدف 218+)، ثم checkpoint ورسالة النتيجة.

**مخاطرة معلومة**: Dialog الطباعة داخل LessonGenerator غير محقق — زر المذكرة ينتقل للصفحة. هذا مقبول.

## حالة مهمة (01:35) — إصلاح competencyAction + إتمام
**المشكلة المكتشفة**: كل sectionCompetencies competencyAction = "إنماء" (الافتراضي). المنطق الحالي في seed-competencies-once.ts (س139-142): إدماج فقط إن كان العنوان/الكفاءة تحوي كلمات إدماج، وإلا "إنماء". **لا يوجد تنصيب إطلاقًا**.
**القاعدة التربوية الصحيحة**: المقطع الأول = تنصيب، الأوساط = إنماء، المقطع الأخير (خاصة إن كان إدماجيًا) = إدماج. يجب تعديل السكربت: action = sectionNumber === 1 ? "تنصيب" : (isIntegration ? "إدماج" : "إنماء").
**ثم**: npx tsx server/seed-competencies-once.ts (one-shot يمسح ويعيد البذر).
**بعد الإصلاح** (نفس الجلسة):
- TS_OK تحقّق، صفحة /competencies تعرض المقاطع مع progress (1AM تاريخ: 25% مقطع1) — لقطة جيدة.
- Competencies.tsx معاد كتابته (~560 سطر) مع: زر طباعة A4 landscape محايد، Dialog استراتيجية التسيير (sectionStrategy)، زر أنشئ مذكرة (يذهب إلى /lesson-generator?subject=&gradeLevel=).
- LessonGenerator.tsx: أضف urlSubject/urlGradeLevel من URL + useEffect جديد (urlInitialized) لتعبئة form عند عدم وجود situationId. TS_OK.
- بقي: إصلاح action في seed ثم seed مجدّد، اختبار pnpm test، توثيق todo.md (3 بنود متبقية [ ])، checkpoint، رسالة للمستخدم.
- السكربت seed: server/seed-competencies-once.ts — DELETE FROM sectionCompetencies ثم INSERT.
- الإجراءات الجديدة في routers: competencyModel.sectionSituations + competencyModel.sectionStrategy (TS_OK).
- اختبار التحقق السابق: 12 نموذج + 36 مقطع.

---

# المهمة الجديدة (20 أوت): مولّد استراتيجية التعلّم النشط بالذكاء الاصطناعي

## الطلب
تطوير محرك الاستراتيجيات ليولّد استراتيجية مخصصة عبر LLM بناءً على السياق التربوي الكامل:
الكفاءة الختامية (termCompetency) + وضع التملك (competencyAction) + الموارد المعرفية (knowledgeResources [{title, action: بناء|إنماء|إدماج}]) + المعايير ومؤشرات التملك (criteria [{criterion, indicators}]) + الحجم الساعي (durationHours) + المادة والمستوى وعنوان الوضعية.
مع حراسة تربوية (لا تطبع خارج الإطار) ومحتوى منهاجي من الوثائق الرسمية فقط، + بقاء الخيار الثابت (fallback).

## الحالة الفنية (مرجع التنفيذ)
- `server/strategies.ts` (252 سطر): محرك ثابت. `suggestStrategyForSituation(input: {title, content?, subject, gradeLevel?})` → SuggestedStrategy {kind, name, rationale, phases[{stage, minutes, teacherRole, studentRole, tips}], totalMinutes, generalTips[]}. `formatStrategyForLesson` للنص. INTEGRATIVE_STRATEGIES + LEARNING_STRATEGIES (3 لكل مادة history/geography/civics).
- `server/routers.ts`:
  - س~2722: `competencyModel.sectionSituations {subject, gradeLevel, sectionNumber}` → getCompetencySectionSituations(activeYear)
  - س~2738: `competencyModel.sectionStrategy {subject, gradeLevel, sectionNumber, situationId?}` → يأخذ الوضعيات، ثم `suggestStrategyForSituation({title: target.title, content, subject: plan?.subject||input.subject, gradeLevel})` — هذا هو موضع الدمج: إضافة mode: static|ai وتمرير السياق الكامل
  - س~2377: `situations.suggestStrategy {id}` للوضعية (يدمج لاحقًا إن أمكن)
- `server/db.ts`:
  - س~1789: `getCompetencySectionSituations(userId, subject, gradeLevel, academicYear, sectionNumber)` — يعيد learningSituations كاملة (sectionId فيها). **تحتاج امتدادًا**: إعادة sectionCompetency والكفاءة الشاملة مع الوضعيات (دالة جديدة getCompetencySectionContext أو تعديل)
  - أعمدة sectionCompetencies: termCompetency, competencyAction, durationHours, durationLabel, criteria(json), knowledgeResources(json), linkedSectionId, competencyModelId
  - competencyModels.globalCompetency (نص الكفاءة الشاملة)
- LLM: `server/_core/llm.ts` — يجب قراءة `/home/ubuntu/skills/webdev-llm-integration/SKILL.md` أولًا
- اختبارات: `server/strategies.test.ts` (12)، المجموع 218/218. checkpoint الحالي: d921aaa8.
- Competencies.tsx (310 سطر): حوار بطاقة الاستراتيجية يستدعي competencyModel.sectionStrategy — إضافة تبديل ثابت/ذكي فيه

## خطة التنفيذ
1. قراءة SKILL.md لـ llm-integration
2. `server/strategies.ts`: إضافة `buildLLMContext(...)` (نص السياق من بيانات المقطع) + مولّد AI بـ structured output (JSON: {name, rationale, phases, totalMinutes, generalTips}) + حراسة (مجموع الدقائق ≤ durationHours*60، kind من detectSituationKind، منع التطبع خارج بيداغوجيات المحرك) + fallback للثابت عند فشل AI
3. `server/db.ts`: دالة جديدة تعيد {sectionCompetency, globalCompetency} لمقطع (عبر competencyModelId + linkedSectionId)
4. `server/routers.ts`: تعديل sectionStrategy لدعم mode='static'|'ai' (افتراضي ai إن كان invokeLLM متاحًا)
5. Competencies.tsx: تبديل ثابت/ذكي + شارة "تم التوليد بالذكاء الاصطناعي" في الحوار
6. اختبارات: اختبار AI بموك invokeLLM + انحدار كامل
7. todo.md + checkpoint + تسليم

## تقدم المهمة (20 أوت — مولّد الاستراتيجية الذكي)
- ✅ أُنجز: `server/strategies.ts` أعيدت كتابته بالكامل:
  - احتفظ بكل المكتبة الثابتة (INTEGRATIVE/LEARNING) + suggestStrategyForSituation + formatStrategyForLesson
  - جديد: `CompetencySectionContext` interface + `buildLLMPrompt(ctx)` (موجّه صارم بالعربية مع ACTION_GUIDANCE للتنصيب/إنماء/إدماج) + `STRATEGY_JSON_SCHEMA` (strict json_schema) + `parseLLMStrategy` + `suggestStrategyWithLLM(ctx)` → {strategy, source: "ai"|"static", note?} مع fallback تلقائي عند الخطأ
  - invokeLLM يُستورد من "./_core/llm" (response_format {type:"json_schema", json_schema:{name, strict:true, schema}}) — response.choices[0].message.content
- المتبقي:
  1. db.ts: دالة `getCompetencySectionContext(subject, gradeLevel, academicYear?, sectionNumber)` تعيد {globalCompetency, termCompetency, competencyAction, durationHours, knowledgeResources, criteria, subject, gradeLevel} — عبر competencyModels(بالمطابقة المرنة للمادة) + sectionCompetencies WHERE competencyModelId=... AND sectionNumber
  2. routers.ts: تعديل competencyModel.sectionStrategy (س~2738) يدعم input.mode?: 'static'|'ai' (افتراضي ai)، يمرر CompetencySectionContext إلى suggestStrategyWithLLM، ويعيد {situation, strategy, source}
  3. Competencies.tsx: في حوار بطاقة الاستراتيجية (سطر 159 strategyQuery) إضافة تبديل ثابت/ذكي وزر تحديث، وشارة "من الذكاء الاصطناعي" عند source='ai'
  4. اختبارات: اختبار strategies.test.ts جديد لـ parseLLMStrategy (مدخلات جيدة/معطوبة) + اختبار buildLLMPrompt + موك invokeLLM في routers.test.ts للوضع ai (لا تنسَ تحديث vi.mock لـ strategies الجديد) — pnpm test يجب أن يمر بالكامل + npx tsc --noEmit
  5. todo.md بند جديد + checkpoint + تسليم
- ملاحظة: routers.test.ts سطرا 77/943 يموككان invokeLLM — يجب إضافة موك strategies.suggestStrategyWithLLM أو فحص كيف يموكّون الآن
- وضع التوليد: يجب ألا يكسر suggestStrategy الحالي (مستخدم في أماكن أخرى) — إضافة فقط

## تقدم لاحق (20 أوت — المرحلة الثانية: دمج AI في router والواجهة)
- أُنجز: getCompetencySectionContext في db.ts؛ تعديل competencyModel.sectionStrategy في routers.ts (mode: static|ai افتراضي static، source + note في الإرجاع)؛ Competencies.tsx dialog أُضيف aiMode + زر توليد AI + إظهار source/note (Brain, Sparkles مستوردة، strategyMode أُزيل).
- TS: يجب التحقق tsc بعد التعديلات الأخيرة.
- المتبقي: اختبارات (buildLLMPrompt + parseLLMStrategy + موك invokeLLM/ai mode) → pnpm test → لقطة → todo.md → checkpoint → تسليم.

## تقدم (الآن): اختبارات المولّد الذكي
- `server/strategies.ts` المعاد بناؤه نهائيًا (252+ سطر):
  - normalizeSubject: history أولًا (السابق geography أولًا — الاختبارات القديمة كانت على النظام القديم)
  - INTEGRATIVE_STRATEGIES: صف واحد (subject=all، "حل الوضعية الإدماجية بمنهجية الأدوات"، 6 مراحل)
  - LEARNING_STRATEGIES: 3 صفوف history(التحقيق التاريخي، 7 مراحل)/geography(الاستكشاف الجغرافي، 6)/civics(الحوار السقراطي، 6)
  - CompetencySectionContext + buildLLMPrompt + STRATEGY_JSON_SCHEMA + parseLLMStrategy (≥4 مراحل) + suggestStrategyWithLLM (fallback static مع note)
- `server/strategies.test.ts` أُضيفت اختبارات جديدة (المجموع 21): إصلاح التوقعات القديمة لتوافق المكتبة الجديدة (التحقيق التاريخي/الاستكشاف/الحوار السقراطي/منهجية الأدوات).
- تبقى: pnpm test (الهدف 218+... عدّ الاختبارات الجديدة)، tsc، لقطة صفحة /competencies (حوار استراتيجية + toggle AI)، todo.md بند للمولّد الذكي، checkpoint، تسليم.
- checkpoint السابق: d921aaa8.

## فشلان متبقيان (01:24)
1. استراتيجية التحقيق التاريخي (سطر 86-92 في strategies.ts): مجموع مراحله 63 (6+12+12+10+10+10+3). التعديل السابق لم يُطبّق على الملف — يجب إعادة تطبيق edit على minutes: 6→5, 10→8, 10→8, 10→7 ليصبح 5+12+12+8+8+7+3=55.
2. اختبار expect(prompt).toContain("الممارسة المستقلة") يفشل: النص في buildLLMPrompt السطر 242: "أكثر توجيهًا في التنصيب، وأقل تدخلًا وأكثر ممارسة مستقلة في الإدماج" — لكنه لا يظهر في الموجّه؟ grep لم يجده أصلًا في strategies.ts — ACTION_GUIDANCE["إدماج"] لا يحويها. يجب إما إضافة "الممارسة المستقلة" إلى ACTION_GUIDANCE["إدماج"] أو تغيير التوقع في الاختبار إلى "أقل تدخلًا" (الموجّه sطر 242 يحويها إذا كانت موجودة في actionGuidance المدمجة في السطر 232: `- وضع التملك: ... — ${actionGuidance}`). الحل: أضف "ممارسة مستقلة" إلى ACTION_GUIDANCE["إدماج"].
اختبارات tsc: لم تظهر أخطاء جديدة في آخر تحقق (التحقق السابق لم يعرض أخطاء). بعد الإصلاحات: checkpoint + todo + تسليم.

## تشخيص حرج (01:25)
- direct test: buildLLMPrompt(ctx).includes("الممارسة المستقلة") = false رغم أن الموجّه المعروض في الخطأ يحتويها!
- السبب المرجح: بناء المصفوفة بـ return [...] لكن هناك return مبكر أو أن المصفوفة تحتوي عناصر null في المنتصف وتُفلتر... لا — filter(Boolean).join("\n") يجب أن يعطي كل شيء.
- الاحتمال الأقوى: الموجّه الذي يُعرض في الخطأ هو من نسخة قديمة من الاستدعاء (من router؟ لا، الخطأ من unit test). الاحتمال: الملف server/strategies.ts الذي يقرأه vitest هو إصدار cache قديم (Vite esbuild cache) لأن الملف عُدّل حديثًا ولم تُمسّ الذاكرة المؤقتة. الحل: حذف node_modules/.vite أو vitest --no-file-parallelism أو إعادة تشغيل server. أو: الدالة buildLLMPrompt فيها return قبل الأسطر 236-246 (قواعد إلزامية صارمة) — أي أن return في منتصف المصفوفة! الأسطر 236-246 قواعد إلزامية تظهر في الخطأ لكن direct-test يعيد false → يجب قراءة نهاية buildLLMPrompt للتأكد: أين ينتهي؟ الأسطر 236-250 هي القواعد ثم filter(Boolean).join. هل يوجد return آخر؟
- المتبقي: قراءة الأسطر 248-260 في strategies.ts، تشغيل vitest مع --clearCache أو cache:false، إعادة الاختبارات، ثم checkpoint وتسليم.

---

# المهمة الجديدة (20 أوت): تنفيذ الاقتراحات الثلاثة

## تم إنجازه (checkpoint 0878a557)
- المولّد الذكي للاستراتيجيات كامل: strategies.ts (buildLLMPrompt، suggestStrategyWithLLM)، db.ts (getCompetencySectionContext)، routers.ts (competencyModel.sectionStrategy mode: static|ai افتراضي static، source+note)، Competencies.tsx (حوار SectionStrategyDialog بـ aiMode وزر توليد AI). 230/230 اختبارًا، TS نظيف، checkpoint 0878a557.

## الخطة الحالية (الاقتراحات الثلاثة)
### 1. دمج وضع AI في بطاقة استراتيجية صفحة اليوم (Dashboard.tsx)
- الحوار: سطر ~933 `Dialog open={strategySituationId !== null}`. الاستعلام: س142 `trpc.situations.strategy.useQuery({id: strategySituationId})` → يجب التحقق مما يعيده (هل يحوي sectionNumber/subject/gradeLevel/termCompetency؟). إن كان يحوي sectionNumber يكفي إضافة state mode واستعلام sectionStrategy ai. إن لم يحوي: إضافة استعلام competencyModel.progress أو استخراج sectionNumber من situation.
- زر الفتح س521: setStrategySituationId(item.nextSituation!.id).
- الحفظ: س1037 saveToLogMutation — يعمل لأي strategy؛ يجب فقط التأكد أن source من dialog (ai/static) — router.savedStrategies.save يقبل source اختياريًا.

### 2. حفظ استراتيجيات AI في دفتر التجارب من صفحة الكفاءات
- Competencies.tsx: Dialog SectionStrategyDialog (~151-260) — يحتوي زر «احفظ في دفتر التجارب» حاليًا؟ (يجب التحقق؛ يبدو يوجد زر HTML لطباعة فقط) — إضافة زر احفظ (saveCustom أو saveStrategyMutation) مع source='ai' وsituationType من kind، subject وgradeLevel وsectionTitle.
- routers.ts: إجراء savedStrategies.save يقبل {strategy, situationType, subject, situationTitle, source?} — التحقق.

### 3. التقرير الفصلي للكفاءات
- router جديد competencyModel.termReport {subject, gradeLevel, term: 1|2|3, academicYear?}: يعيد للمقاطع [sectionNumber, termCompetency (مختصر 80 حرفًا), competencyAction, durationHours, situations: {total, completed, ids}, masteryPercent, weak: boolean] + إجمالي + توصيات (مقاطع إنجازها < 75%).
- db.ts: دالة جديدة تعيد التقدم لكل مقطع (استخدام getCompetencyProgress + getCompetencySectionSituations لكل مقطع) + ربط byTerm (مقاطع كل فصل حسب mappng المقطع→الفصل: مقطع1→فصل1، مقطع2→فصل2، مقطع3→فصل3).
- Competencies.tsx: زر «التقرير الفصلي» (select الفصل) يفتح Dialog بجدول النسب + زر طباعة A4 محايد (buildCompetencyTermReportPrintHtml بنفس نمط buildPrintHtml).
- اختبار db-mock جديد + انحدار كامل.

## نقاط مرجعية
- checkpoint السابق: 0878a557 (المولّد الذكي)، قبله d921aaa8 (نموذج الكفاءات).
- Owner: OWNER_OPEN_ID = knc56o2qrPzkhmR37CjpWy.
- بعد الإنجاز: pnpm test + npx tsc --noEmit + تحديث todo.md (3 بنود [ ] → [x]) + checkpoint + رسالة للمستخدم.

## تقدم (الاقتراح 1 — صفحة اليوم): التشخيص
- الوضعيات تحوي sectionId → annualPlanSections.sectionNumber → competencyModel.sectionStrategy {subject, gradeLevel, sectionNumber, mode:"ai"}.
- الخطة: في suggestStrategy (router س~2377): جلب section → annualPlanSections للحصول على sectionNumber وannualPlanId → الخطة للحصول على subject/gradeLevel → استدعاء competencyModel.sectionStrategy داخليًا بدلًا من إعادة التنفيذ.
- إرجاع {strategy, source, note} من situations.suggestStrategy.
- Dashboard.tsx: إضافة state aiMode (useState) + استعلام situations.suggestStrategy {id, mode} (يجب تمرير mode!) + شارة AI + زر الحفظ موجود أصلًا (saveToLogMutation س295) — لا تعديل لازم عليه.
- Competencies.tsx: تحقق هل حوارها يحوي زر حفظ (اقتراح 2): إن لم يحوِ، أضف saveToLogMutation (موجود في AnnualPlanDetail س57 وDashboard س295).

## خطة التنفيذ النهائية (للاقتراحات الثلاثة) — مرجع
1. **الاقتراح 1 (صفحة اليوم)**: تعديل `situations.suggestStrategy` (routers.ts س2378) يستقبل `mode` اختياريًا، يجلب `annualPlanSections` عبر situation.sectionId → subject/gradeLevel/sectionNumber → يستدعي `getCompetencySectionContext` + `suggestStrategyWithLLM` داخليًا عند mode=ai، وإلا المطابقة الثابتة. يعود {strategy, source, note}. في Competencies.tsx حوار الاستراتيجية يدعم mode=ai أصلًا (تأكد). في Dashboard.tsx حوار س933: أضف state strategyMode + تبديل، وأضف Badge للمصدر.
2. **الاقتراح 2 (حفظ الذكاء الاصطناعي في الدفتر)**: Competencies.tsx حوار الاستراتيجية يفتقد زر الحفظ — أضف saveToLogMutation (نمط Dashboard س295: subject من gradeLevel/subject المعادين من progress، situationTitle = مقطع الكفاءة الختامية). تأكد أن source يحفظ source/ai في جدول savedStrategies (حقل source موجود؟ فحص schema savedStrategies).
3. **الاقتراح 3 (التقرير الفصلي)**: إجراء competencyModel.quarterlyReport {subject, gradeLevel, academicYear, quarter(1|2|3)} — يجمع: نسب تملك المقاطع (getCompetencyProgress + sectionCompetencies)، الوضعيات المنجزة/المتبقية لكل مقطع، توصيات (المقاطع تحت 50% تحتاج مراجعة، المقاطع المكتملة جاهزة للاختبار)، وربط بالتقويم التحصيلي الموحّد. صفحة `/competencies` تبويب «التقرير الفصلي» + زر طباعة A4 محايد.
4. جدول savedStrategies schema: أعمدة source/sourceType؟ — فحص قبل التعديل.
5. بعد التنفيذ: pnpm test (230/230 حاليًا) + tsc + لقطة شاشة + checkpoint + تحديث todo.md.

## حالة التنفيذ (20 أوت — مرحلة 2)
- routers.ts: عدّلت `situations.suggestStrategy` — يستقبل `mode: "fixed"|"ai"` (افتراضي fixed)، يجلب section → plan، وعند ai يستدعي `getCompetencySectionContext(subject, gradeLevel, sectionNumber)` ثم `suggestStrategyWithLLM({...context, situationTitle, subject as Subject, gradeLevel as GradeLevel})`. الإرجاع {strategy, source: "ai"|"static", note?}.
- **متبقي في routers**: إضافة Subject و GradeLevel للاستيرادات — الأنواع من "../shared/types" (فحص أين Subject/GradeLevel معرّفة في shared/types.ts).
- savedStrategies: لا عمود ai/llm — source enum فقط "engine"|"custom" (لا نعدل schema، زر الحفظ يحفظ الاستراتيجية نفسها في الحالتين).
- Competencies.tsx: حوارها يدعم mode=ai أصلًا (Toggle وزر ولّد) — تم التحقق سابقًا. زر حفظ فيها؟ لا (grep لم يجد "احفظ في دفتر التجارب") — أضف saveToLogMutation (نمط Dashboard.tsx س295).
- Dashboard.tsx س933-1060: حوار الاستراتيجية يستخدم `trpc.situations.suggestStrategy.useQuery({id})` — أضف state strategyMode مع Toggle وزر تبديل بين "المطابقة الثابتة"/"توليد ذكي" (يعيد الاستعلام بـ {id, mode})، وأضف شارة مصدر AI، والحفظ يعمل أصلًا (saveToLogMutation س295).
- التقرير الفصلي (اقتراح 3): إجراء competencyModel.quarterlyReport — لاحقًا في المرحلة 3.
- بعد التنفيذ: pnpm test + tsc + لقطات + checkpoint.

## حالة حرجة (20 أوت — 63 خطأ TS)
- السبب: `situations.suggestStrategy` أعيد بناؤه ليعيد `{strategy, source, note}` بينما الاستخدامات القديمة (AnnualPlanDetail س510-598، Dashboard س305-344 + 951-1030) تقرأ الحقول مباشرة (.name, .phases...).
- الحل الموحد المختار: في كل موقع استخدام أضف helper محلي: `const s = strategyQuery.data?.strategy || (strategyQuery.data as any);` — لا، الأنظف: في موقع الاستعلام أضف `.select(d => d.strategy)` في useQuery بحيث يبقى كل ما بعده سليمًا ويُفقد source (نقبل ذلك في AnnualPlanDetail لأنه ثابت دائمًا)؟ لا — AnnualPlanDetail يستدعي competencyModel.sectionStrategy (س52) الذي أعيد بناؤه أيضًا. **الخيار الأنظف**: استخراج helper مشترك `resolveStrategy(data)` يعيد {strategy: SuggestedStrategy, source}. لكن الأنسب لتفادي refactors واسعة: في AnnualPlanDetail س52-75 (strategyQuery = trpc.competencyModel.sectionStrategy.useQuery) أضف `select: (d) => d.strategy` إذا كانت select مدعومة بـ trpc. trpc useQuery يقبل select! هذا يحل دون لمس 50 سطرًا.
- ملاحظة: Dashboard.tsx س142 (strategyQuery لحوار اليوم) يستدعي `trpc.situations.strategy`؟ (فحص س142) — يجب التحقق: ربما يستدعي situations.suggestStrategy {id, mode} أصلًا → نفس الحل select.
- Competencies.tsx حوار الاستراتيجية يستخدم competencyModel.sectionStrategy أيضًا (س~151) — فحص هل كُتب أصلاً على الشكل الجديد {strategy, source} (كان يُقرأ بعد التعديل في المولّد الذكي) — يجب عدم كسره.
- remaining: 1) إصلاح موقعي AnnualPlanDetail + Dashboard بالـ select، 2) إضافة mode إلى Dashboard، 3) زر حفظ في Competencies.tsx، 4) التقرير الفصلي، 5) اختبارات.
